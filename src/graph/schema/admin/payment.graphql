extend type Query {
    paymentPagination(
        page: Int!
        limit: Int!
        order_by: String
        search: Map
    ): PaymentPagination!
    paymentTotalByDay(
        month: String!
        year: String!
    ): TotalAmountPayment!
}
